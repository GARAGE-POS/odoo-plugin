<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Scheduled Action for External Order Sync -->
        <record id="ir_cron_sync_external_orders" model="ir.cron">
            <field name="name">Sync External Orders</field>
            <field name="model_id" ref="model_external_order_sync"/>
            <field name="state">code</field>
            <field name="code">model.cron_sync_orders()</field>
            <field name="interval_number">15</field>
            <field name="interval_type">minutes</field>
            <field name="active" eval="True"/>
        </record>
    </data>
</odoo>

